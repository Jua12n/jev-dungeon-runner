# Server-only. Do not prefix with VITE_, or it will be exposed in the browser.
JEV_API_KEY=your_jev_api_key_here
JEV_MODEL=jev-latest

# Optional: force mock decisions in the API while keeping the same frontend flow.
# JEV_MODE=mock
